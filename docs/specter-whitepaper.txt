SPECTER

Ignotus Nemo

Version 1.0.5
25 March, 2026

/****/ Introduction /****/

I call my proposal Specter because, like a ghost, authorization in this
system exists but cannot be seen. There are no signatures. No public keys.
Only proofs of ownership-bound authorization that leave no trace.

Bitcoin requires every node to download and verify every signature ever
made. This is already painful. With post-quantum cryptography, signatures
become 30-40 times larger. The chain bloats to absurdity. Worse, every
signature reveals a public key, which reveals identity linkage. Privacy
dies in plain sight.

Specter takes a different path. I do not compress signatures. I
eliminate them entirely.

This document is a research whitepaper and design specification
draft. It describes Specter's cryptographic architecture, security
model, and engineering targets. It is not a complete byte-level
protocol specification. Serialization formats, wire protocol
details, and finalized constraint layouts are deferred to the
implementation specification. Performance numbers are engineering
targets unless explicitly marked as benchmarked.

/****/ The Core Idea /****/

Instead of proving "this signature is valid for this key," I prove:

  "A transaction was authorized by knowledge of a secret consistent
   with an on-chain ownership commitment, bound to this specific
   transaction, with replay protection."

This is a semantic shift. Authorization becomes a zero-knowledge
statement about ownership, not a verification of a signature algorithm.

The insight comes from observing what signatures actually do:

  1. Prove knowledge of a secret key
  2. Bind that knowledge to a specific message
  3. Prevent replay (via nonces or UTXO consumption)

All three can be achieved with hash-based zero-knowledge proofs.
No elliptic curve discrete log. No lattice problems. Just hashes.

/****/ Ownership Without Keys /****/

Each user holds a Root Entropy Value (REV). This is their master secret.
From it, I derive everything:

  Master Secret (REV, 256 bits):
    |
    +-- id_root     = BLAKE3-KDF(REV, "specter.id-root")
    +-- spend_root  = BLAKE3-KDF(REV, "specter.spend-root")
    +-- scan_root   = BLAKE3-KDF(REV, "specter.scan-root")
    +-- null_root   = BLAKE3-KDF(REV, "specter.nullifier-root")
    +-- auth_root   = BLAKE3-KDF(REV, "specter.auth-root")

  Each root serves exactly one protocol role:
    id_root:    Derives coinbase ownership commitments
    spend_root: Receiver-side anchor in stealth ownership derivation
    scan_root:  Derives scanning key pair for output detection
    null_root:  Derives nullifiers for spend prevention
    auth_root:  Derives per-transaction authorization bindings

  Authorization:         Auth = H(auth_root || tx_hash || "specter.auth")
  Nullifier (2-stage):   sk_note = H(null_root || rho || "specter.note-null")
                         N = H(sk_note || note_cm || "specter.nullifier")

The primary ownership primitive is the per-output owner_commitment,
derived from the stealth protocol for regular transactions. The
id_root is used only for coinbase outputs (which have no stealth
sender). No persistent "identity" goes on-chain. The REV never
leaves the user's control. No single derived root can reconstruct
any other, preventing cross-protocol leakage.

All keys are deterministically derived from the REV alone. A user
who loses their device but retains their REV (or mnemonic encoding
of it) can fully reconstruct all derived roots, regenerate their
ML-KEM scan key pair, rescan the chain, and recover all unspent
notes. This is the wallet recovery guarantee.

When spending, the user proves in zero knowledge. The spend
authorization relation covers:

  C1. "I know a spending secret such that the owner commitment matches"
  C2. "I have bound this proof to this specific transaction"
  C3. "My nullifier is correctly derived in two stages from null_root
       and the note, and is fresh (not used before)"
  C4. "All domain tags are consistent"

These four constraints form the core spend authorization. Full
transaction validity additionally requires:

  V1. Input existence (each input note_cm exists in the UTXO
      accumulator at the claimed state root)
  V2. Value conservation (sum of input values = sum of output
      values + fee)
  V3. Range validity (all values non-negative, bounded to 64 bits,
      no overflow)
  V4. State-root consistency (all inputs proven against the same
      UTXO root; all nullifier non-membership proofs against the
      same nullifier root)
  V5. Canonical encoding (note serialization uniqueness, no
      alternate encodings producing the same semantic object)

The user generates this proof locally. The proof, not the secret,
is what enters the network. The REV never leaves the user's device.

/****/ Note Model /****/

A note (UTXO) in Specter is a tuple:

  note = (owner_commitment, value_commitment, rho, memo_ciphertext)

Where:
  owner_commitment  = H(owner_secret || "specter.owner")     (32 bytes)
  value_commitment  = H(v || r || "specter.value")           (32 bytes)
  rho               = per-note randomness (256-bit, unique)
  memo_ciphertext   = Enc(output_key, v || counter)          (~48 bytes)

The note commitment (the canonical identifier in the UTXO set) is:

  note_cm = H(owner_commitment || value_commitment || rho || "specter.note")

This single value enters the MMR accumulator. The nullifier is bound
to it:

  sk_note = H(null_root || rho || "specter.note-null")
  nullifier = H(sk_note || note_cm || "specter.nullifier")

The on-chain output for a stealth transaction contains:

  - note_cm           (32 bytes)
  - owner_commitment  (32 bytes)
  - value_commitment  (32 bytes)
  - KEM ciphertext    (1,088 bytes -- ML-KEM-768)
  - view_tag          (1 byte)
  - memo_ciphertext   (~48 bytes)

Total per output: ~1,233 bytes.

For coinbase outputs (no stealth sender), the KEM ciphertext and
view_tag are absent. The coinbase owner_commitment is derived
from id_root:

  coinbase_owner = H(id_root || height || "specter.coinbase")

Total per coinbase output: ~144 bytes.

Coinbase outputs form a distinct, weaker privacy domain. They
lack a sender-generated stealth channel by necessity. Ownership
is derived deterministically from id_root and block height. An
observer cannot link coinbase outputs to each other without
knowing id_root. But if REV is ever compromised, all historical
coinbase outputs for that miner become retroactively identifiable.

Ordinary stealth outputs remain compartmentalized: each has its
own owner_secret derived from an ephemeral KEM shared secret, so
REV compromise does not directly reveal which stealth outputs
belong to the user without also rescanning the chain.

This asymmetry is a conscious design trade-off. Coinbase outputs
require deterministic recoverability without a sender. The cost
is structurally weaker privacy for miner rewards. When a miner
subsequently spends multiple coinbase-derived notes, the spending
pattern may create a statistical fingerprint. Miners concerned
about this should consolidate coinbase outputs through stealth
self-transfers before spending.

/****/ Why STARKs /****/

I reject trusted setup. This is non-negotiable.

Groth16 requires ceremonies. BN254 curves are not quantum-safe.
These are disqualifying properties for a system meant to last.

STARKs are transparent. No trusted setup. No pairings. Just hash
functions and polynomial commitments via FRI. Under standard
assumptions about collision-resistant hashing, they provide
post-quantum security.

The trade-off: STARK proofs are larger than SNARKs. A single
per-transaction proof will be 30-50 KB. This is larger than a
post-quantum signature (Dilithium at 2.4 KB, SPHINCS+ at 8-49 KB).
I accept this trade-off because the proof carries more than a
signature ever could: it proves ownership, authorization, balance
conservation, range validity, and nullifier correctness. All in one.
No public key is ever revealed. No identity is ever linked.

The cost is manageable. Each user generates a 30-50 KB proof when
sending a transaction. Nodes verify each proof individually.
Preliminary estimates suggest verification under 5 ms per proof,
meaning a block of 500 transactions would take about one second
of parallel verification on commodity hardware. These numbers are
engineering targets pending final circuit benchmarks.

/****/ Proving System: Plonky3 over BabyBear /****/

I use Plonky3 as the proving backend with the BabyBear field
(p = 2^31 - 2^27 + 1 = 2,013,265,921). This is a deliberate
departure from earlier designs that considered Winterfell over
Goldilocks. The reasons:

  1. BabyBear is a 31-bit field. Field multiplications take ~0.4
     cycles with SIMD (AVX-512 or NEON). Goldilocks is 64-bit,
     roughly 2-4x slower per operation. Every field operation in
     trace building, FFT, constraint evaluation, and FRI benefits
     from this.

  2. Plonky3 has native GPU support via ICICLE (CUDA, Metal, CPU
     backends). ICICLE-Plonky3 achieves 3-7x speedup over CPU SIMD
     for NTT and constraint evaluation. Winterfell has no GPU path.

  3. The ecosystem is consolidating around BabyBear. SP1 (Succinct),
     RISC Zero, and Polygon Miden (migrating from Winterfell to
     Plonky3) all use BabyBear. Building on the same field means
     access to optimized libraries, audited code, and ongoing
     community investment.

  4. Plonky3 supports six hash functions for Merkle commitments
     (Poseidon2, Poseidon, Blake3, Keccak, Rescue, Monolith). I
     use Poseidon2 for in-circuit hashing and Blake3 for Merkle
     tree commitments, a hybrid approach that reduces proving time
     by ~50% for the commitment phase while keeping the circuit
     arithmetic-friendly.

  5. BabyBear requires a degree-4 (quartic) extension for 128-bit
     security (4 x 31 = 124 bits). This is constructed as a tower
     of two quadratic extensions. Most prover work stays in the
     31-bit base field. Extension field operations are used only
     for challenge generation and FRI folding.

  6. BabyBear's two-adicity is 2^27, supporting NTT domains of up
     to 134 million rows. More than sufficient for any single
     transaction trace.

The performance implications are concrete. On an Apple M3 Pro,
Plonky3 proves ~1.7 million Poseidon2 hashes per second. A
Specter transaction proof requires roughly 100-140 Poseidon2
evaluations in-circuit (ownership hashes, nullifier derivations,
Merkle paths, value commitments). At this rate, proving time
targets 2-5 seconds on a modern multi-core CPU. I will not claim
"under 1 second" until benchmarks on the final circuit prove it.

/****/ A Note on Zero Knowledge /****/

Standard STARK constructions provide computational integrity but not
zero knowledge by default. The proof may leak information about the
witness. For a privacy system, this is unacceptable.

Plonky3 is a modular toolkit. It provides succinctness and soundness,
but the zero-knowledge property must be explicitly constructed. I
achieve this through the rigorous masking technique formalized by
Habook and Al Kindi (ePrint 2024/1037):

First, witness masking: for each trace column polynomial, I add a
random low-degree polynomial that vanishes on the trace domain H.
This randomizes polynomial evaluations outside H -- crucially, on
the larger LDE domain where Merkle commitments are made -- without
affecting constraint satisfaction within H.

Second, DEEP composition polynomial masking: an additional random
low-degree polynomial is added to the final DEEP composition
polynomial, preventing information leakage through the constraint
evaluation channel.

The number of masking coefficients h is determined by:

  h >= 2 * S * (e * n_D + n_F) + n_F

Where S is the quotient splitting factor (2), e is the extension
field degree (4 for BabyBear), n_D is the number of DEEP queries
(1), and n_F is the number of FRI queries (~50 at our security
level). This yields h ~ 500 random coefficients -- negligible
compared to trace lengths of thousands of rows.

The overhead is under 5% additional proving time and negligible
proof size increase.

With both masking layers applied, the underlying IOP achieves
honest-verifier zero knowledge (HVZK). The Fiat-Shamir
transformation replacing verifier challenges with hash function
outputs - upgrades HVZK to full computational zero knowledge in
the Random Oracle Model. Because the hash function produces
unpredictable, non-manipulable challenges, a malicious verifier
cannot adaptively choose challenges to extract information.

This is not hand-waved. The construction follows Habook-Kindi's
proof, which explicitly treats the subtlety of quotient polynomial
decomposition - "a source for mistakes, both in literature as well
as in software implementations."

Private witness data (REV, blinding factors, values) is transformed
through hash evaluations before entering the trace. The trace
contains witness-derived intermediate states, not the raw secrets.
The zero-knowledge property described above ensures that these
intermediate states are computationally hidden from the verifier:
the masking polynomials randomize all committed evaluations outside
the trace domain, making the published commitments indistinguishable
from random under the hash function's preimage resistance.

/****/ User-Side Proving /****/

Each user generates their own STARK proof when creating a transaction.
This is the critical architectural decision that distinguishes Specter
from systems where block producers generate proofs.

When a user creates a transaction, their wallet:

  1. Builds an execution trace for the single transaction
  2. Includes all constraints (C1-C4, balance, range proofs)
  3. Applies witness masking and DEEP composition masking
  4. Generates a STARK proof (~30-50 KB)
  5. Discards the witness data (REV, values, blinding factors)
  6. Broadcasts the transaction WITH the proof attached

The proven transaction enters the network. It contains:

  - Input commitments (what is being spent)
  - Output commitments (what is being created)
  - Nullifiers (to prevent replay)
  - The STARK proof of validity

No witness data. No private information. The proof attests to
correctness without revealing anything about the user's identity
or the transaction values.

This design eliminates the privacy leak inherent in miner-side
proving, where the block producer must see all witness data. In
Specter, the only entity that ever sees the REV is the user's
own wallet.

Proof generation targets 2-5 seconds on a modern multi-core CPU
with Plonky3 over BabyBear. For GPU-equipped machines, 1-3 seconds
is achievable via ICICLE. For low-powered devices, proof
generation can be delegated to a trusted prover service, though
this requires sharing witness data with that service. The
trade-off is the user's to make.

The proof statement for a single transaction covers the spend
authorization relation (C1-C4) and the value validity constraints
(V2-V3):

  - Ownership claim is valid (C1)
  - Authorization is bound to this transaction (C2)
  - Nullifiers are correctly derived (C3)
  - Domain tags match (C4)
  - Sum of input values equals sum of output values plus fee (V2)
  - All values are in valid range (non-negative, 64-bit) (V3)

Input existence (V1) and state-root consistency (V4) are verified
outside the STARK via MMR inclusion proofs and nullifier
predecessor proofs checked by the consensus layer.

/****/ Transaction Canonicalization and Authorization Binding /****/

The authorization digest tx_hash must bind the proof to the exact
transaction semantics. If any field that affects spend semantics is
excluded, the protocol is vulnerable to malleability, rebind attacks,
or output mutation.

tx_hash covers the following fields, concatenated in canonical
fixed-length encoding:

  tx_hash = H(
    version                    ||
    input_note_commitments     ||
    input_nullifiers           ||
    utxo_accumulator_root      ||
    nullifier_tree_root        ||
    output_note_commitments    ||
    output_owner_commitments   ||
    output_value_commitments   ||
    output_kem_ciphertexts     ||
    output_view_tags           ||
    output_memo_ciphertexts    ||
    fee                        ||
    "specter.txhash.v1"
  )

Auth = H(auth_root || tx_hash || "specter.auth")

What is covered: all inputs, all outputs, fee, state roots against
which membership and non-membership are proven, and all stealth
delivery data. A change to any covered field invalidates the proof.

What is not covered: the STARK proof itself (the proof attests to
the digest, not vice versa). Block-level fields (height, timestamp,
PoW nonce) are not part of the transaction digest; they belong to
the block header.

What is mutable by relays: nothing. All transaction fields are
covered by tx_hash and thus by the STARK proof. Relays cannot
modify any field without invalidating the proof.

Future versions may extend the digest with additional fields
(locktime, expiry, fork id). The version prefix and domain tag
allow non-ambiguous upgrades.

/****/ Multi-Input Privacy /****/

When a user spends multiple UTXOs in a single transaction, all
nullifiers appear together, revealing they are controlled by the
same spender. This is the common-input-ownership linkage that
Zcash's Sapling protocol also exhibits.

I mitigate this through two mechanisms:

First, each UTXO has its own note-local spending witness
(owner_secret), not the master REV. The proof demonstrates
knowledge of owner_secret for each input independently. An
observer sees multiple nullifiers in the same transaction but
cannot determine whether they come from the same master identity
or multiple cooperating parties.

Second, the wallet implements a coin selection policy that minimizes
multi-input transactions. When possible, a single UTXO of sufficient
value is selected. The wallet avoids gratuitous consolidation.

For users requiring stronger unlinkability, individual transactions
can be submitted separately at different times, each spending a
single input. The trade-off is higher total fees and multiple proof
generation rounds.

This is an honest acknowledgment: multi-input transactions within
a single proof do create a statistical linkage signal. Perfect
unlinkability in multi-input transactions requires either dummy
inputs (as in Zcash Orchard's action model) or separate proofs.
Both are possible future extensions.

/****/ Block Production /****/

Proven transactions arrive at the mempool. Nodes verify each proof
before acceptance. Invalid proofs are rejected immediately.

Transaction validation follows a cost-escalating ladder:

  1. Structural checks (nanoseconds): canonical encoding, size
     caps, field bounds. Reject malformed data before parsing.
  2. Semantic checks (microseconds): duplicate nullifiers, input/
     output count limits, fee minimum. Reject obviously invalid
     transactions before touching the proof.
  3. STARK proof verification (~1-5 ms): the expensive step.
     Only reached by structurally and semantically valid
     transactions.

This ordering ensures that cheap garbage cannot force expensive
proof verification. Peers that repeatedly submit transactions
failing at stage 1 or 2 are deprioritized.

The block producer collects verified transactions, packages them
into a block, and searches for a valid PoW nonce. The block
contains the transactions with their individual proofs. Verifying
nodes check each per-transaction proof (parallelizable, under 5 ms
each).

The block producer does not need witness data. It does not need to
re-prove any transaction. It only verifies existing proofs and
assembles the block. No trust required.

/****/ Consensus Validation Semantics /****/

A block commits to the following state:

  - utxo_root: MMR root of all unspent note commitments
  - nullifier_root: indexed Merkle tree root of all spent nullifiers
  - block_header: height, timestamp, prev_hash, utxo_root,
    nullifier_root, tx_root, pow_nonce, target

Block headers commit to post-state roots: after all transactions
in the block have been applied.

Transactions within a block are validated against the block's
pre-state (the parent block's post-state). All transactions in the
same block share the same input state roots. This means:

  - Two transactions in the same block cannot spend the same output
    (duplicate nullifier check across the block's transaction set).
  - A transaction cannot spend an output created in the same block
    unless intra-block ordering is enforced and state roots are
    updated incrementally. In the initial design, same-block
    spend of same-block outputs is not permitted. Outputs become
    spendable in the next block.

Consensus validation for each transaction consists of:

  1. Verify the per-transaction STARK proof (covers C1-C4 and
     V2-V3: ownership, authorization, nullifier correctness,
     domain separation, balance, and range validity).
  2. Verify UTXO accumulator inclusion proofs for each input
     (MMR membership against the claimed utxo_root). These proofs
     are provided alongside the transaction, outside the STARK.
  3. Verify nullifier non-membership via low-nullifier predecessor
     proofs against the claimed nullifier_root. These proofs are
     also outside the STARK.
  4. Check that the claimed utxo_root and nullifier_root match
     the parent block's post-state roots.
  5. Check nullifier uniqueness across the block (no two
     transactions in the same block may produce the same nullifier).

After all transactions are validated, the node applies them
deterministically:

  - Append new note_cm values to the UTXO MMR.
  - Insert new nullifiers into the indexed Merkle tree.
  - Compute post-state roots.
  - Verify post-state roots match the block header commitment.

Transaction ordering within a block is canonical (ordered by
tx_hash). This ensures deterministic state application across
all nodes.

/****/ Non-Interactive Transactions /****/

Specter uses a hybrid stealth address scheme. The classical approach
uses elliptic curve Diffie-Hellman, which is not post-quantum safe.
Pure hash-based non-interactive key exchange is not known to exist
under classical assumptions (Impagliazzo-Rudich). I accept a minimal
compromise: ML-KEM-768 (NIST FIPS 203) for the stealth delivery
channel only. All authorization remains entirely hash-based.

Each receiver holds secrets derived from their seed:

  spend_root:  Used to derive spending authority (from REV KDF tree)
  scan_root:   Used to derive scanning key (from REV KDF tree)

From scan_root, the receiver generates a PQ key pair:

  scan_keypair = ML-KEM-768.KeyGen(scan_root)
  scan_pk = scan_keypair.public_key     (1,184 bytes)
  scan_sk = scan_keypair.secret_key     (private)

The receiver publishes a stealth meta-address:

  SMA = (scan_pk, H(spend_root))

The sender creates a one-time output:

  1. (ciphertext, shared_secret) = ML-KEM-768.Encapsulate(scan_pk)
  2. output_key = BLAKE3-KDF(shared_secret, "specter.stealth-output")
  3. view_tag = first_byte(output_key)
  4. owner_secret = BLAKE3-KDF(output_key || H(spend_root) || counter,
                                "specter.owner-derive")
  5. owner_commitment = H(owner_secret || "specter.owner")

The owner_commitment serves as the ownership commitment for this
output. The KEM ciphertext (1,088 bytes) and view_tag (1 byte) go
on-chain alongside the output commitment.

The sender encrypts the value and counter using ChaCha20-Poly1305
keyed by output_key as the stealth memo.

Why ML-KEM and not pure hashes? Because non-interactive key exchange
requires asymmetric primitives. This is a theoretical wall, not a
design choice. I accept it for three reasons:

  1. The KEM is used ONLY for stealth delivery. All authorization,
     commitment, nullifier, and proving logic remains hash-based.

  2. If ML-KEM is ever broken, funds cannot be stolen because
     spending authority is entirely hash-based. However, recipient
     privacy may be weakened: an attacker who recorded historical
     KEM ciphertexts could retrospectively decapsulate them,
     recovering stealth shared secrets and linking outputs to
     receivers. The severity depends on the nature of the break.

  3. ML-KEM-768 is NIST-standardized (FIPS 203). If lattice assumptions
     fail, HQC (code-based, expected FIPS 2027) can be substituted.

/****/ View Tags: Post-Decapsulation Filtering /****/

Without optimization, the receiver must attempt full KEM
decapsulation and key derivation for every output on-chain.
ML-KEM-768 decapsulation costs ~0.1ms per output. At 500
outputs per block and 720 blocks per day, that is 360,000
decapsulations per day -- approximately 36 seconds of CPU
time. Tolerable for a desktop wallet, painful for mobile.

The view tag reduces this. The 1-byte view_tag is derived
from the KEM shared secret:

  view_tag = first_byte(BLAKE3-KDF(shared_secret, "specter.stealth-output"))

The receiver decapsulates each output's KEM ciphertext to
obtain a candidate shared_secret, derives the candidate
view_tag, and checks against the on-chain value:

  - If mismatch (probability 255/256 = 99.6%), skip further
    derivation. The decapsulation cost is already paid, but
    the expensive owner_secret derivation, memo decryption,
    and commitment verification are skipped.
  - If match (probability 1/256), proceed with full derivation.

This saves approximately 60-70% of scanning CPU time by
avoiding the full key derivation chain on non-matching outputs.

The security property is critical: view_tag is derived from
the KEM shared secret, which only the sender (who knows r)
and the receiver (who knows scan_sk) can compute. An outside
observer cannot compute view_tag for a given output without
breaking ML-KEM. This is the fundamental difference from a
tag derived purely from public data, which would be a
recipient tagging oracle.

The receiver scans:

  For each output:
    1. Attempt ML-KEM-768.Decapsulate(ciphertext) -> shared_secret
    2. Derive candidate view_tag from shared_secret
    3. Compare against on-chain view_tag
    4. If mismatch, skip (99.6% of outputs)
    5. Derive output_key, owner_secret, owner_commitment
    6. Check if owner_commitment matches on-chain value
    7. If match: this output belongs to them
    8. Decrypt memo, recover value and counter

To SPEND the output, the receiver uses owner_secret as the
note-local spending witness. The receiver proves C1 (knowledge
of owner_secret such that owner_commitment is valid) in zero
knowledge.

This scheme uses ML-KEM for key exchange and hashes for everything
else. Post-quantum safe by construction.

/****/ Value Commitments /****/

I use hash-based commitments instead of Pedersen commitments:

  C = H(v || r || "specter.value")

Where v is the value (64-bit unsigned integer), r is the blinding
factor (256-bit), and the domain tag provides separation from other
uses of the hash function. All inputs are fixed-length encoded.

This is computationally hiding and computationally binding under
collision resistance of H. Post-quantum safe.

The cost: I lose additive homomorphism. I cannot check
Sum(inputs) = Sum(outputs) by simply adding commitment points.
Instead, balance verification happens inside the STARK proof.
The prover demonstrates in zero knowledge that the committed
values satisfy the conservation equation. The verifier never
sees the values, only the proof.

This is simpler than it sounds. The STARK already processes
the transaction. Adding a running sum constraint costs a
handful of additional trace columns and constraints.

Range proofs also move inside the STARK. Each value is
decomposed into bits within the execution trace, and the
constraints verify that the bit decomposition is correct
and the value is non-negative. This replaces Bulletproofs
or similar external range proof systems with a few hundred
constraints per value.

The architectural cost extends beyond proving overhead. Without
additive homomorphism, future features that depend on algebraic
commitment structure become harder: collaborative transactions
(CoinJoin-style), payment channels with efficient state updates,
batched multi-party spend construction, and recursive proof
aggregation over balance claims. These are not impossible with
hash-based commitments, but they require the balance relation
to be re-proved inside each STARK rather than checked cheaply
via commitment algebra. This is a permanent tax on protocol
composability, accepted in exchange for genuine post-quantum
security without algebraic assumptions.

The result: no elliptic curves anywhere in the authorization
system. The entire protocol is hash-based for authorization
and commitment. Post-quantum security is not a retrofit. It
is the foundation. (The stealth channel uses ML-KEM as
acknowledged above.)

/****/ The Nullifier Set /****/

Each spent output produces a nullifier via two-stage derivation:

  sk_note = H(null_root || rho || "specter.note-null")
  N = H(sk_note || note_cm || "specter.nullifier")

Where null_root is derived from the REV (not directly the REV
itself), rho is per-note randomness (unique), note_cm is the full
note commitment, and domain tags ensure separation.

The two-stage derivation provides:

  1. Note-specific secret: sk_note is unique per note (via rho)
     and cannot be linked across notes without null_root.

  2. Nullifier binding: N commits to both the note secret and the
     note commitment, preventing cross-note confusion.

The nullifier is deterministic: the same output always produces
the same nullifier when spent by the same owner. But an observer
cannot compute the nullifier without knowing null_root, so they
cannot determine which output was spent.

Nullifiers are stored in an indexed Merkle tree. When spending,
the user proves inside their STARK proof that the nullifier is
correctly derived AND that it does not already exist in the tree
(non-membership via the low-nullifier predecessor proof).

Why indexed Merkle trees instead of sparse Merkle trees? A sparse
Merkle tree with 256-bit keys requires 256 hash operations per
non-membership proof inside a ZK circuit. An indexed Merkle tree
uses a sorted linked-list structure where non-membership is proven
by showing a "low nullifier" whose value is less than the target
and whose next-pointer points to a value greater than the target.
This requires only a membership proof of the low nullifier (at
tree depth ~32) plus a range check. Roughly 8x fewer hashes.

Both Aztec and Nomos have independently arrived at this same
conclusion. When two teams solving the same problem converge on
the same answer, pay attention.

The nullifier construction is designed to satisfy:

  - Uniqueness: each spendable note produces exactly one valid
    nullifier. No two distinct valid witnesses yield different
    nullifiers for the same note (enforced by deterministic
    derivation from null_root, rho, and note_cm).
  - Unlinkability: an observer cannot determine which note_cm
    corresponds to a given nullifier without knowing null_root.
  - Note-binding: the nullifier commits to note_cm, preventing
    an attacker from producing a valid nullifier that corresponds
    to a different note.
  - Collision resistance: under collision resistance of H, no
    adversary can produce two distinct notes that yield the same
    nullifier (which would allow one spend to silently invalidate
    another note).

This prevents double-spending without revealing which output was
spent. Only the owner knows the mapping from commitment to nullifier.

/****/ Security and Privacy Goals /****/

Specter aims to provide the following properties against a passive
chain observer (an adversary who sees all on-chain data but does
not control the user's device or network layer):

  1. Sender anonymity: an observer cannot determine which prior
     output is consumed by a given nullifier.
  2. Receiver unlinkability: an observer cannot link an output to
     its intended recipient without the recipient's scan key.
  3. Amount hiding: committed values are computationally hidden.
  4. Authorization unlinkability: proofs from the same user across
     different transactions are not linkable.
  5. Post-quantum spend soundness: under collision resistance of
     the hash function and soundness of the STARK, no adversary
     (including a quantum one) can forge a valid spend proof.
  6. Post-quantum stealth confidentiality: under ML-KEM security,
     stealth delivery is confidential against quantum adversaries.

Specter does NOT aim to provide:

  1. Network-layer anonymity (IP-level sender detection requires
     Tor, Dandelion++, or similar transport-layer protection).
  2. Mempool origin hiding.
  3. Timing or transaction-graph obfuscation.
  4. Multi-input unlinkability (multiple inputs in a single
     transaction are linkable as same-spender; see Multi-Input
     Privacy section).
  5. Coinbase privacy equivalent to stealth privacy (coinbase
     outputs form a weaker privacy domain; see Note Model).
  6. Recovery privacy after REV compromise (a compromised REV
     allows retroactive identification of all coinbase outputs
     and, with chain rescanning, stealth outputs).
  7. Protection against a malicious proving service (delegated
     proving requires sharing witness data).
  8. Resistance to endpoint compromise (malware on the user's
     device can extract the REV).

This enumeration is intended to be exhaustive for the current
design. Claims of privacy in this paper should be interpreted
within this adversary model.

/****/ Chain Growth: The Critical Problem /****/

A naive design where every proof is stored forever is fatal. At
500 transactions per block with 50 KB proofs each, that is 25 MB
of proofs per block. At 720 blocks per day (120-second block time),
that is 18 GB per day of proof data alone. No blockchain survives
this.

I solve this through four complementary mechanisms:

  1. Proof Pruning
  2. Cut-Through
  3. UTXO Set Accumulator (Utreexo-style)
  4. Tiered Node Architecture

Together, these reduce Specter's steady-state chain growth to
well below Bitcoin's ~250 MB/day.

/****/ Proof Pruning /****/

A STARK proof exists to convince verifiers that a state transition
is valid. Once the transition is accepted into consensus and buried
under sufficient proof of work, the proof has served its purpose.
It is redundant.

After N confirmations (N = 1440, approximately 48 hours of blocks),
full nodes may discard the proof portion of each transaction. The
verified result -- the state transition itself (nullifiers added,
outputs committed) -- is permanent. The proof that justified it is
ephemeral.

What remains per transaction after proof pruning depends on
whether stealth data (KEM ciphertexts) is also pruned.

Stealth data (KEM ciphertexts, memos) is needed for wallet
recovery from seed. A receiver who has already scanned does
not need it again. There are two pruning tiers:

  Tier 1 -- Proof pruning (after 1440 confirmations):
    Discard STARK proofs. Stealth data retained.

    Per transaction (2-in/2-out):
      - Input nullifiers:                     64 bytes
      - Input note_cm references:             64 bytes
      - Output note_cm + owner + value cm:   192 bytes
      - Output KEM ciphertexts:            2,176 bytes
      - Output view_tags + memos:             98 bytes
      - Transaction kernel (fee):             48 bytes
      - Total per transaction:            ~2,642 bytes

  Tier 2 -- Stealth data pruning (after extended period):
    Discard KEM ciphertexts and memos. Only commitments and
    nullifiers remain. Wallet recovery from this point forward
    requires archive nodes.

    Per transaction (2-in/2-out):
      - Input nullifiers + references:       128 bytes
      - Output commitments:                  192 bytes
      - Transaction kernel:                   48 bytes
      - Total per transaction:              ~368 bytes

Compare to pre-pruning: ~368 bytes + ~2,274 bytes stealth data
+ 30-50 KB proof = ~33-53 KB.

Tier 1 pruning reduces storage by 92-94%.
Tier 2 pruning reduces storage by 99%.

At Tier 1 (proofs pruned, stealth data retained):
  500 txs/block = ~1.3 MB permanent data per block.
  720 blocks/day = ~940 MB/day.

At Tier 2 (full pruning):
  500 txs/block = ~184 KB permanent data per block.
  720 blocks/day = ~133 MB/day.

The honest accounting: with KEM-based stealth, the permanent
chain is larger than previously stated. Tier 1 growth (~940
MB/day) is approximately 4x Bitcoin's rate. Tier 2 growth
(~133 MB/day) is below Bitcoin, but requires archive nodes
for late wallet recovery.

The practical middle ground: retain stealth data for a recovery
window (e.g., 90 days), then prune. Users who restore wallets
after the window need an archive node. This is comparable to
Bitcoin's assumeUTXO trust model.

New nodes syncing from genesis have two options:

  1. Full sync: Download all blocks including proofs (available
     from archive nodes), verify every proof, then prune. This
     is the trustless path.

  2. Checkpoint sync: Download a verified UTXO set snapshot at
     a recent checkpoint, download block headers from genesis to
     verify the PoW chain, then sync recent blocks with proofs.
     This is analogous to Bitcoin's assumeUTXO.

Archive nodes retain all proofs permanently for historical
auditability. They are not required for consensus participation.

An important consequence of proof pruning: historical validity
becomes an archival availability assumption, not a universally
verifiable property. A new node syncing after the pruning window
cannot independently verify pruned transactions without obtaining
proofs from archive nodes. Ongoing consensus remains trustless
(all recent proofs are available and verified). Historical full
verification requires that at least one archive node preserves
the proof data. This is the same trust model as Bitcoin's
assumeUTXO, applied to proof data rather than UTXO snapshots.

In short: Specter preserves trustless ongoing consensus, but
trustless historical re-verification is optional and archive-
dependent after the pruning horizon.

/****/ Cut-Through /****/

If an output is created and spent within the same block,
both can be eliminated:

  Before: A -> B -> C
  After:  A -> C

The intermediate output B never needs to exist on-chain. This
reduces block size and state bloat.

Cross-block cut-through during pruning is more constrained than
in Mimblewimble. An intermediate output that has been created and
spent may have its commitments removed from the UTXO set, but its
stealth data (KEM ciphertext, memo) may still be needed by a
receiver who has not yet scanned that region of the chain. Intra-
block cut-through is clean. Cross-block cut-through of commitment
data is safe only after the stealth data retention window has
passed.

Over time, the permanent chain state converges toward:

  - Block headers
  - Transaction kernels (fee records)
  - Unspent output commitments
  - The nullifier set

This is structurally similar to Mimblewimble's long-term state.
The difference: Specter's stealth metadata introduces a retention
dependency that Mimblewimble does not have. Cross-block cut-through
is achievable but bounded by the stealth data retention window.

/****/ UTXO Set Accumulator /****/

The UTXO set grows monotonically (new outputs minus spent outputs).
Bitcoin's UTXO set is ~11 GB as of 2025 with ~173 million entries.
Storing the full set is burdensome for resource-constrained nodes.

Specter uses a Merkle Mountain Range (MMR) accumulator for the
UTXO set, inspired by Utreexo. Full nodes maintain the complete
UTXO set and MMR. Compact nodes store only the MMR roots -- less
than 1 KB of data -- and verify inclusion proofs provided by peers
or by the spender.

When spending a UTXO, the wallet includes a short MMR inclusion
proof (~500 bytes) alongside the transaction, outside the STARK
proof. The verifier checks membership separately from the STARK.
This avoids the proving cost explosion of embedding full Merkle
path verification inside the arithmetic circuit. The STARK covers
ownership, authorization, nullifier, balance, and range constraints.
The MMR inclusion proof covers input existence.

This creates a spectrum of node types:

  - Archive nodes: Full blocks, all proofs, full UTXO set
  - Full nodes: Full blocks (pruned proofs), full UTXO set
  - Compact nodes: Block headers + UTXO MMR roots (<1 KB state)
  - Light nodes: Block headers only (SPV-equivalent)

/****/ Chain Growth Summary /****/

                      Before pruning   Tier 1 pruned    Tier 2 pruned   Bitcoin
Per-block data        ~27 MB           ~1.3 MB          ~184 KB         ~1.7 MB
Per-day growth        ~19 GB           ~940 MB          ~133 MB         ~250 MB
Per-year growth       ~7 TB            ~343 GB          ~49 GB          ~91 GB
5-year chain size     ~35 TB           ~1.7 TB          ~243 GB         ~450 GB

At Tier 1 (stealth data retained), Specter's chain growth is
approximately 4x Bitcoin's rate. This is the cost of KEM-based
stealth privacy. At Tier 2 (full pruning with archive-dependent
recovery), growth falls below Bitcoin.

The practical recommendation: run Tier 1 with a 90-day stealth
data retention window. Archive nodes retain everything permanently
for late wallet recovery and historical auditability.

/****/ Concrete Hash Function: Poseidon2 over BabyBear /****/

Throughout this paper, H denotes Poseidon2 over the BabyBear field
(p = 2^31 - 2^27 + 1 = 2,013,265,921). Poseidon2 was chosen for
three reasons:

  1. ZK-friendly: ~126 constraints per invocation inside a STARK
     over BabyBear (width 16, rate 8, 8 external rounds, 13
     internal rounds, S-box x^7). Up to 4x fewer constraints than
     original Poseidon.

  2. Security: 128-bit collision resistance with well-studied
     algebraic properties. The quartic extension of BabyBear
     provides ~124 bits of field security.

  3. Performance: 31-bit field multiplications are ~4x cheaper
     than 64-bit (Goldilocks) on standard hardware. The 13
     partial rounds (vs 22 for Goldilocks Poseidon2) compound
     this advantage.

For Merkle tree commitments during proving, Blake3 is used for
the upper tree levels (not re-proved inside the STARK). Poseidon2
is used for leaf-level hashing (proved inside the STARK). This
hybrid approach reduces commitment time by approximately 50%.

For non-ZK uses (symmetric encryption in stealth hints, general
data hashing, block header hashing), Blake3 is used for its
native speed (~3-8 GB/s single-threaded).

/****/ Consensus /****/

Proof-of-work using double Blake3. Simple. Fast to verify.
Consistent with the rest of the protocol: hash-based, auditable,
no exotic dependencies.

Blake3 is already in Specter's dependency tree for Merkle
commitments and general-purpose hashing. Using it for PoW adds
zero new dependencies. Verification takes nanoseconds, not
milliseconds -- header sync becomes I/O-bound, not CPU-bound.
The implementation is roughly 50 lines, not 10,000. It is
trivially auditable.

The trade-off: Blake3 PoW favors GPUs and eventually ASICs.
But consider what actually rate-limits transaction spam in
Specter. It is not PoW. It is per-transaction STARK proof
generation (~2-5 seconds of CPU time per transaction). The PoW's
job is block-level Sybil resistance and emission scheduling,
not transaction spam prevention. For that purpose, a simple
hash-based PoW is sufficient. Bitcoin secures a trillion-dollar
network with double-SHA256 -- about as simple as it gets.

The PoW function:

  pow_hash = Blake3(Blake3(header_bytes || nonce))

Valid if pow_hash < target. That is all.

The block producer:

  1. Collects proven transactions from the mempool
  2. Verifies each per-transaction STARK proof
  3. Assembles block with proven transactions
  4. Finds valid Blake3 nonce
  5. Broadcasts block

The miner does not generate individual transaction proofs. It does
not see witness data. It does not know who authorized what. It only
knows that each transaction's proof verified successfully.

Proof generation cost is borne by the sender. Generating a valid
STARK proof costs 2-5 seconds of CPU time with Plonky3 over
BabyBear. This raises the marginal cost of transaction creation,
providing a natural rate limit on honest wallet issuance. However,
proof generation cost alone is not a complete anti-spam mechanism:
adversaries with dedicated GPU clusters or proof-farming
infrastructure can amortize this cost. Fee-market-based spam
resistance remains necessary as a complementary mechanism.

Block time: 120 seconds. This provides room for proof verification,
block propagation across the network, and reduces chain growth.
At 500 transactions per block, the sustained throughput is about
4 transactions per second.

Difficulty adjustment: ASERT (aserti3-2d), proven mathematically
to converge under any positive hash rate elasticity. The formula:

  next_target = anchor_target * 2^((time_delta - 120 * (height_delta + 1)) / halflife)

With halflife = 172,800 seconds (2 days). Implemented using
fixed-point integer arithmetic to avoid floating-point
non-determinism. For every 2 days ahead of schedule, difficulty
doubles. ASERT has been battle-tested since November 2020 on
Bitcoin Cash.

/****/ Emission Schedule /****/

Specter uses a smooth exponential decay with a perpetual tail
emission, modeled after Monero's approach but with parameters
suited to a 120-second block time.

Initial block reward: 50 SPEC.

The block reward decreases smoothly each block (no halvings, no
supply shocks):

  reward(height) = max(floor(50 * 0.999999 ^ height), 0.6)

This produces a smooth decay curve. The reward reaches the floor
of 0.6 SPEC per block after approximately 4.4 million blocks
(~16.8 years at 720 blocks/day). The tail emission then continues
indefinitely at 0.6 SPEC per block.

Key properties:

  - Total pre-tail supply: ~49.4 million SPEC
  - Tail emission: 0.6 SPEC/block = 432 SPEC/day = ~158K SPEC/year
  - Year 1 inflation: high (bootstrapping phase)
  - Year 10 inflation: ~1.8% (still in decay phase)
  - Year 20 inflation: ~0.3% (tail phase)
  - Approaches 0% asymptotically

The tail emission ensures miners always have baseline revenue
independent of fee markets. The smooth decay avoids the halving
shocks that create speculative cycles. At ~158K SPEC/year against
a mature supply of ~50M, the tail inflation rate of ~0.3% may
fall below the long-term coin loss rate, making Specter
potentially net-deflationary in practice.

No premine. No founder's reward. No ICO. The first block reward
goes to the first miner who solves it.

/****/ What I Achieve /****/

No signatures on-chain. Ever.

No authorization public keys revealed in spend validation. KEM
ciphertexts appear on-chain for stealth delivery, but no key
used for spending verification is ever exposed.

No addresses. Outputs are commitments. Recipients scan for theirs.

No elliptic curves in authorization. The entire authorization
system is hash-based.

Post-quantum security by construction, not by retrofit.

No trusted setup.

Non-interactive transactions via PQ-KEM stealth addresses with
view tag scan optimization.

Verification target: under 5 ms per transaction proof,
parallelizable. (Preliminary estimate from AIR sketch; not yet
benchmarked on the final circuit.)

Proving target: 2-5 seconds per transaction on modern multi-core
CPU. (Engineering target, pending final AIR and prover
implementation.)

View tags for post-decapsulation filtering (derived from KEM shared
secret, not from public data). The KEM decapsulation cost is always
paid; view tags reduce the downstream derivation and verification
work on non-matching outputs.

Proof pruning for managed chain growth. Tiered pruning from ~940
MB/day (stealth data retained) to ~133 MB/day (full pruning).

UTXO accumulator for compact nodes (<1 KB state).

Cut-through compression.

Privacy by default. Witness data never leaves the user's device.

User-side proving. The sender generates the proof. The network
only verifies.

Fair emission. No premine, smooth decay, perpetual tail reward.

ASERT difficulty adjustment with mathematical convergence guarantees.

/****/ Questions I Woke Up Sweating About /****/

Q: What if someone loses their REV?

A: They lose their funds. This is the same as losing a Bitcoin
   private key. Social recovery schemes could help, but they
   add complexity I leave for future work.

Q: How do you handle light clients?

A: Light clients verify block headers and the PoW chain. They
   trust the longest chain without downloading full transaction
   data or verifying individual proofs. This is comparable to
   SPV in Bitcoin. Compact nodes go further: they verify UTXO
   inclusion proofs against the MMR root in the block header.

Q: What about scripts and conditions?

A: Not in version one. If there is demand, I could add simple
   conditions (timelocks, hashlocks) by extending the proof
   statement. But simplicity is a feature.

Q: Why Plonky3 over BabyBear instead of Winterfell over Goldilocks?

A: Three reasons. First, BabyBear field operations are 2-4x
   faster than Goldilocks on the same hardware (31-bit vs 64-bit
   SIMD). Second, Plonky3 has GPU support via ICICLE; Winterfell
   does not. Third, the ecosystem is consolidating around Plonky3
   and BabyBear (SP1, RISC Zero, Miden). Winterfell's own
   flagship project (Miden VM) is migrating away from it.

Q: Is proof generation too slow for users?

A: With Plonky3 over BabyBear, a per-transaction STARK proof
   targets 2-5 seconds on a modern multi-core CPU. On GPU, 1-3
   seconds via ICICLE. This is slower than typing a recipient
   address but fast enough for practical use.

Q: Is mining decentralized?

A: Miners do not generate proofs -- users do. The miner's job
   is to verify proofs (cheap), assemble blocks, and find a valid
   Blake3 nonce. Mining with Blake3 will favor GPUs and eventually
   ASICs. This is an accepted trade-off: the protocol's anti-spam
   mechanism is per-transaction STARK proof generation, not PoW
   complexity. Mining centralization is an economic problem, not
   an algorithm problem.

Q: You dropped Pedersen commitments. What do you lose?

A: Additive homomorphism. I can no longer check balance
   conservation by adding commitment points. Instead, balance
   checks happen inside the STARK proof. This adds some proving
   cost but removes all elliptic curve dependencies. The
   trade-off is worth it for genuine post-quantum security.

Q: You dropped elliptic-curve stealth addresses. What do you lose?

A: Compact key exchange. I replaced ECDH with ML-KEM-768 (NIST
   FIPS 203) for post-quantum safety. The KEM ciphertexts are
   larger (~1 KB per output) and scanning requires per-output
   KEM decapsulation (~0.1ms each). View tags reduce post-decap
   work by ~99.6%. The cost is real but manageable. The KEM is
   used only for stealth delivery, not for authorization.

Q: Per-transaction proofs are 30-50 KB. Isn't that too large?

A: Larger than a post-quantum signature, yes. But each proof
   replaces not just a signature - it replaces a signature, a
   range proof, a balance proof, and a nullifier proof. All in
   one object. And the proof buys you something signatures never
   could: complete privacy.

   More importantly, proofs are pruned after 48 hours. The
   permanent per-transaction footprint depends on the pruning
   tier: ~2.6 KB with stealth data retained, ~368 bytes after
   full pruning. Both are far smaller than the original proof.

Q: What about the chain size? 30-50 KB proofs per transaction
   sounds like it will balloon out of control.

A: This was the biggest problem in the original design. Without
   pruning, 500 txs/block at 50 KB proofs = 25 MB/block =
   18 GB/day. That is unsurvivable.

   With proof pruning (discard proofs after 1440 confirmations),
   cut-through, and UTXO accumulator, chain growth drops to
   ~940 MB/day with stealth data retained, or ~133 MB/day
   after full pruning. The KEM ciphertexts are the dominant
   permanent cost -- this is the honest price of PQ stealth
   privacy.

   Proofs exist on the network for ~48 hours, during which they
   are verified by full nodes and miners. After that, they are
   discarded. The chain keeps the results, not the proofs.

/****/ Future Directions /****/

Proof aggregation. Recursive STARK composition could compress
per-transaction proofs into a single block-level proof, reducing
verification cost. However, this requires the aggregator to process
all individual proofs, which introduces a trust point. Whether this
trade-off is acceptable depends on whether the aggregator can be
kept from learning anything beyond "this proof is valid."

Proof markets. Users who lack hardware for proof generation can
outsource to competing provers. This introduces a privacy trade-off
(the prover sees the witness), but trusted execution environments
could mitigate this.

State channels. Off-chain transactions with on-chain settlement proofs.

STARK-to-SNARK wrapping. For use cases requiring constant-size
verification (e.g., cross-chain bridges), wrap the STARK proof in
a SNARK. This preserves transparency during proving while achieving
compact verification.

Dummy inputs for perfect multi-input unlinkability. Following
Zcash Orchard's action model, each transaction action could contain
exactly one spend and one output, with dummy actions
indistinguishable from real ones. This would eliminate the
statistical linkage signal from multi-input transactions.

GPU-assisted proving in browser via WebGPU. All major browsers now
support WebGPU (Chrome, Firefox, Safari, Edge), and zkSecurity has
demonstrated 2-5x proving speedups in browser. A web wallet could
generate Specter proofs entirely client-side with no native
installation required.

Purely hash-based non-interactive key exchange for stealth. The
theoretical barrier (Impagliazzo-Rudich) suggests this may not exist
classically. Quantum OT constructions from symmetric primitives
offer a possible future path.

Formal verification of the AIR constraints. Machine-checked proof
that the constraint system correctly enforces the intended
transaction semantics.

/****/ Conclusion /****/

Bitcoin gave us decentralized money. Privacy coins showed us how to
hide the addresses and amounts. Specter shows us how to hide the
authorization itself.

A signature says: "I possess the key that matches this public key."
A Specter proof says: "I am authorized." Nothing more. The chain
sees only commitments and proofs. Identity is spectral - present
but invisible.

The proofs are temporary. The privacy is permanent. With aggressive
pruning and archive-dependent recovery, steady-state chain growth
can fall below Bitcoin. Without it, privacy has a storage cost.
This is an honest trade-off, not a free lunch.

This is not an incremental improvement. This is a conceptual shift.
Authorization is not signature verification. Authorization is a
statement about identity, proven in zero knowledge by the user,
verified by every node, visible to no one.

The specter is already here. You just cannot see it.


/****/ References /****/

[1]  Nakamoto, S. "Bitcoin: A Peer-to-Peer Electronic Cash System" (2008)
[2]  Jedusor, T.E. "Mimblewimble" (2016)
[3]  Maxwell, G. "Confidential Transactions"
[4]  Ben-Sasson, E. et al. "Scalable, transparent, and post-quantum
     secure computational integrity" (STARKs)
[5]  Grassi, L. et al. "Poseidon2: A Faster Version of the Poseidon
     Hash Function" (2023)
[6]  Aztec Protocol. "Indexed Merkle Trees for Nullifier Sets"
[7]  O'Connor, J. et al. "BLAKE3: one function, fast everywhere" (2020)
[8]  Polygon Miden. "Client-side proving with STARKs" (2024)
[9]  Habook, U. and Al Kindi. "A note on adding zero-knowledge to
     STARKs" (ePrint 2024/1037, revised Feb 2025)
[10] Dryja, T. "Utreexo: A dynamic hash-based accumulator optimized
     for the Bitcoin UTXO set" (MIT DCI, 2019)
[11] Plonky3. Polygon Labs. https://github.com/Plonky3/Plonky3
[12] Ingonyama. "AIR-ICICLE: Plonky3 on ICICLE" (2025)
[13] ERC-5564. "Stealth Addresses" (Ethereum, 2023)
[14] Monero Research Lab. "View tags for reducing wallet sync time"
     (MRL Issue #73, 2022)
[15] Nethermind. "STARKPack: Aggregating STARKs for shorter proofs
     and faster verification" (2024) [future work reference]
[16] NIST FIPS 203. "Module-Lattice-Based Key-Encapsulation Mechanism
     Standard (ML-KEM)" (2024)
[17] Impagliazzo, R. and Rudich, S. "Limits on the provable
     consequences of one-way permutations" (STOC 1989)
[18] Kawaguchi et al. "ASERT ensures convergence to a stationary
     distribution under any positive elasticity" (2025)
[19] Bitcoin Cash. "ASERT Difficulty Adjustment Algorithm (aserti3-2d)"
     (HF-20201115, November 2020)
[20] Zcash Protocol Specification. "Orchard Action Model and Nullifier
     Design" (NU5, 2022; updated 2025)


- ---

Ignotus Nemo
(Latin: "Unknown Nobody")
